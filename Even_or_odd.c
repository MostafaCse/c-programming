#include <stdio.h>
int main()
{
    int Number;
    printf("Enter the value: ");
  scanf("%d",&Number);

    if (Number%2==0)
        printf("The number is even");
    else {
        printf("The number is odd");
        }
        return 0;
}
